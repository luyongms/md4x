/target
.DS_Store

# Auto-generated PDFs from the dogfooding hook (docs only).
# Template preview PDFs under templates/<name>/preview.pdf are
# committed as test baselines and explicitly NOT ignored.
docs/**/*.pdf
dist/

# RTK per-user config
.rtk/

# Tauri build-time generated schemas (regenerated each build)
crates/md4x-gui/gen/

# Personal scratch / sample input + output
/misc/

# Throwaway sync-probe exploration harness
/scratch/

# Design handoff drops (large, regenerable from upstream design project)
/design_handoff_*/

# Stray build / archive artifacts
/md4x.zip
